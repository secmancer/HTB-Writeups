- **Goal**: Identify the web app, enumerate thoroughly, and obtain valid creds for foothold on **Nibbles (Linux, Easy, 10.10.10.75)**.
- **Initial web check**:
    - `whatweb 10.129.42.190` → Apache/2.4.18 on Ubuntu; index shows **“Hello world!”**.
    - Page source reveals HTML comment: **`/nibbleblog/`**.
- **App identification**:
    - `whatweb http://10.129.42.190/nibbleblog/` → **Nibbleblog** (PHP), HTML5, jQuery; session cookie present.
    - **Gobuster** on `/nibbleblog/` finds: `/admin`, **`/admin.php` (200)**, `/content`, `/languages`, `/plugins`, `/themes`, `/README`.
    - `curl .../README` → **Nibbleblog v4.0.3 (Coffee)**. Known **authenticated file-upload RCE** exists for this version (Metasploit module).
- **Admin portal reality check**:
    - `/admin.php` login needed; brute-force blocked (**Blacklist protection**).
    - Common creds fail; password reset triggers email error.
- **Directory listing & sensitive files**:
    - `/nibbleblog/themes/` and `/nibbleblog/content/` are browsable.
    - `curl .../content/private/users.xml` → confirms **username = admin**; shows blacklist entries.
    - Root dir brute-force shows nothing else useful (`/index.html`, `/server-status` 403).
    - `curl .../content/private/config.xml` → site/title strings include **“Nibbles” / “Yum yum”**; potential password clue.
- **Credential inference**:
    - Using context (box name, config strings, notification emails), deduce likely admin password **`nibbles`**.
    - Successful admin login → Nibbleblog dashboard (publish/manage/settings), confirming foothold path.
- **Exploit path (next)**:
    - With valid creds, leverage **Nibbleblog 4.0.3 authenticated file upload** to upload PHP webshell / get reverse shell.
- **Key lessons**:
    - Iterative enumeration (view source, wordlists, tech fingerprinting) > immediate exploitation.
    - Directory listing + config/metadata often leak **usernames** and **password clues**.
    - Brute-force controls demand **creative pivoting** (content-derived passwords) instead of hammering.
    - Save outputs and keep notes (`-oA`, curl dumps) to speed analysis and reporting.